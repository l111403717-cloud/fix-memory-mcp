interface:
  display_name: "Fix Memory Workflow"
  short_description: "跨窗口组装核心上下文，并复用已验证的修复记忆"
  default_prompt: "Use $fix-memory-workflow to assemble my scoped operating context, follow active project policy, and use deeper fix retrieval only when the task has a real error or repeated issue."
